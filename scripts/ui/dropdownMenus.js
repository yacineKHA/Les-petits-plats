export function dropdrownMenus () {
    const dropdrownMenus = document.querySelectorAll('.dropdown-menu');
    
    const ingredientsDropdown = dropdrownMenus[0];
    const appliancesDropdown = dropdrownMenus[1];
    const ustensilsDropdown = dropdrownMenus[2];    
    
}